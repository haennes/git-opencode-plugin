{
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    "opencode-git-tools",
    "~/.config/opencode/plugins/opencode-git-tools.ts"
  ],
  "instructions": [
    "Prefer git* plugin tools (gitStatus, gitDiff, gitCommit) over raw git bash commands."
  ]
}
